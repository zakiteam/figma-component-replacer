# Shared Data

This folder contains structured data used by shared helpers and runtime code.

Use this area for static data that should be imported rather than hardcoded in Vue components or Figma runtime files.

Keep data modules side-effect free.
