# Global UI Styles

This folder contains global UI styles only.

`base.css` defines reset rules and design tokens as CSS custom properties. Component layout and visual styling should live beside each component instead of being added here.

Use this folder sparingly. If a style belongs to a component, put it in that component's `style` folder.
