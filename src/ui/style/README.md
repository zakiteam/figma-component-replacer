# App Styles

This folder contains styles for the root Vue app component.

Use this area for layout owned by `App.vue`, such as the overall shell, sidebar, summary area, and main content grid.

Do not place child component styling here. Child components should keep their styles inside their own `style` folder.
